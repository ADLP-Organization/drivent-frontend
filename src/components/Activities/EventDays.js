import ActivitiesByDay from './ActivitiesByDay';

export default function EventDays() {
  return (
    //'oi, aqui é pra selecionar o dia e, se o dia estiver selecionado, exibir o componente ActivitiesByDay'
    <ActivitiesByDay/> //
  );
};
